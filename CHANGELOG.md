# Changelog

All notable changes to this project will be documented in this file.
The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [1.2.0] - 2026-09-02

### Added
* **괴이 매트릭스 접기/펼치기(Accordion) 토글 지원**: 상단 제어 바에 아코디언 토글 버튼(`▲ 괴이 매트릭스 접기` / `▼ 괴이 매트릭스 펼치기`)을 추가하여 세로 공간 절약 및 가독성 개선 (`aria-pressed`, `aria-label` 등 웹 접근성 준수)
* **`FilterManager` 통합 상태 관리기 (`src/content/filter-state.js`)**: 괴이 레벨(EX1~EX9), 괴이 소재, 이름 검색어(수식어 파싱 포함)를 단일 파이프라인에서 통합 평가하는 상태 관리 엔진 구축 (구독-발행 패턴 지원)
* **분리된 전용 스타일시트 (`src/content/ui.css`)**: 템플릿 리터럴 내 인라인 `<style>` 태그를 완전히 제거하고 독립된 CSS 파일로 분리하여 Manifest V3 CSP 준수 및 렌더링 성능 최적화
* **전용 고화질 아이콘 에셋 (`icons/`)**: 선브레이크 크림슨/와인 엠블럼과 골드 테두리 스타일의 표준 PNG 아이콘 3종(`icon-16.png`, `icon-48.png`, `icon-128.png`) 추가
* **Zero-dependency 단위 테스트 스위트 (`tests/`)**: Node.js 내장 테스트 러너(`node --test`) 기반으로 외부 의존성 없이 31개 단위 테스트 구축 (데이터 무결성, 필터 엔진, 파서, Manifest 구조)

### Changed
* **1,400+ 라인 정적 몬스터 데이터셋 분리**: 대용량 메타데이터를 `src/content/monsters.data.js`로 분리하여 Node.js/브라우저 환경 범용 호환 및 모듈성 강화
* **필터링 DOM 조작 일원화**: `search.js`와 `ui-form.js`에서 각자 처리하던 DOM 가시성 제어 로직을 `FilterManager` 파이프라인으로 일원화하여 필터 상태 불일치 버그 원천 차단
* **테이블 렌더러 CSS 유틸리티 클래스 전환**: `ui-table.js`의 인라인 스타일 속성을 `ui.css` 유틸리티 클래스로 교체

### Fixed/Security
* **최소 권한 원칙(Least Privilege) 적용**: `manifest.json`에서 미사용 `"storage"` 권한을 제거하고 필수적인 `"tabs"` 권한만 유지하여 확장 프로그램 보안 및 개인정보보호 강화
* **백그라운드 서비스 워커 URL 매칭 방어 로직 강화**: `startsWith` 검사를 도입하여 URL 쿼리스트링/해시 파라미터가 포함되어 있어도 탭 이동을 정상 처리
* **백그라운드 배지 배경색(`setBadgeBackgroundColor`) 적용**: 다크/라이트 테마 모두에서 선명하게 식별되도록 `#be123c` 크림슨 배경색 명시
* **DOM Null Safety 가드 강화**: `common.js`에 안전한 폴백 래퍼를 적용하여 인벤 웹페이지 구조 변경 시에도 스크립트 런타임 크래시 방지

---

## [1.1.0] - 2026-08-17

### Added
* **괴이탐구 검색창 실시간 상태 바**: 현재 적용된 필터 상태 태그, 검색 결과 카운트(`결과: N마리`), 원클릭 `↺ 전체 초기화` 버튼 추가
* **이름 검색창 문법 안내 팁**: 입력창 하단에 `/`, `!`, `@`, `#` 고급 검색 문법 도움말 및 플레이스홀더 제공 (12px)
* **몬스터 테이블 상단 특효/속성 가이드 바**: 몬스터 테이블 직전에 속통룡주/약특속성 및 물리 특효주 범례 설명 바 추가 (12px)
* **괴이 레벨별 전용 컬러 플로팅 뱃지**: 몬스터 썸네일 좌상단에 상단 매트릭스 테마와 동기화된 `Ex 1~9` 등급별 전용 컬러(핑크/스카이블루/인디고/바이올렛) 플로팅 뱃지 부착
* **단계별 괴이 소재 화살표 흐름(Stepped Flow)**: 몬스터 이름 하단에 `소재1 ➔ 소재2 ➔ 소재3` 형태로 파밍 진화 단계 시각화 (중앙 정렬)
* **주요 팁 코멘트 뱃지**: 몬스터별 특수 기믹(예: `💡 풍압 ➔ 독 유효`)을 단정한 앰버/골드 뱃지로 중앙 정렬 렌더링 (12px)

### Changed
* **속성 정보 일원화 및 수직 일렬 정렬**: 인벤 기존 `유효속성` 열 내 각 속성 라인 우측 끝에 속통/약특 이모지(`🌠`, `⭐`, `⚠️`, 12px)를 수직 일렬(`Flex Space-between`)로 직접 병합
* **'특효' 컬럼 슬림화**: 4칸 `❌` 나열을 없애고 실제로 유효한 물리 특효주(`공: ⭐`, `룡: ⭐`)만 1~2칸으로 컴팩트하게 축소
* **괴이 소재 화살표 시인성 보강**: 회색 줄무늬 배경 행에서도 또렷하게 식별되도록 슬레이트 그레이(`#64748b`) 및 볼드 가중치 적용
* **전체 폰트 및 이모지 밸런스 최적화**: 인벤 표 톤앤매너에 맞추어 요소별 폰트 크기(10~12px) 및 세로/중앙 정렬 정돈
* **Chrome Extension 표준 모던 디렉토리 구조 리팩토링**:
  * 소스코드를 `src/background/`, `src/content/` 하위로 일원화
  * 파일명에서 `_1.0.0`, `_1.0.1` 버전 접미사 일괄 제거 (`common.js`, `monsters.js`, `ui-form.js`, `ui-table.js`, `search.js`)
  * `괴이탐구.txt` 메모 파일을 `docs/` 디렉토리로 이동
  * 미사용 구버전 레거시 파일 정리

---

## [1.0.2] - 2026-03-28
### Added
* Speckit AI 에이전트 가이드라인 및 프로젝트 헌법 문서 도입

### Changed
* 몬스터 검색 데이터 구조 `Map` 적용을 통한 성능 최적화 ($O(N) \to O(1)$)
* UI 테이블 렌더링 시 중복 상수 선언 개선

---

## [1.0.1] - 2022-11-29
### Added
* 괴이 소재 추가 및 UI 변경

### Changed
* 몬스터 이름 검색 방법 변경
* 기타 오류 수정

---

## [1.0.0] - 2022-10-15
### Changed
* 검색창 UI 변경

---

## [0.8.0] - 2022-09-01
### Fixed
* 공략속성 추천 오류 수정 및 데이터 타입 변경

---

## [0.7.3] - 2022-08-20
### Changed
* 괴이 소재 검색 버튼 인게임 소재 정렬과 동기화

---

## [0.7.2] - 2022-08-15
### Fixed
* 원망 마가도 정보 수정
* 검색 시 화면 스크롤 위치 조정

---

## [0.7.1] - 2022-08-10
### Added
* 속성 육질에 따라 이모지 추가

---

## [0.7.0] - 2022-08-07
### Added
* 속성 육질 정보 추가
